import { NavBarContainer, MusicChart } from "./Nav.style";

const Nav = () => {
  return (
    <NavBarContainer>
      <MusicChart>
        기상송 차트
      </MusicChart>
    </NavBarContainer>
  );
};

export default Nav;
